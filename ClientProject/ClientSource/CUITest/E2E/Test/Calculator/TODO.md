make grid calculate cell rects precisely without those stupid gaps
make grid use min size
Calculator should be focusable as a whole
It should change theme when focused
It should accept key input when focused
text should be in readonly textinput
separate buttons, mb add some grid gap and grid border props